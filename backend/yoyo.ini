# Local-dev defaults for the yoyo migration tool.
# In containers / prod, pass the DB explicitly: yoyo apply --database "$DATABASE_URL" ./migrations
[DEFAULT]
sources = migrations
database = postgresql://stroke:stroke@localhost:5432/stroke
batch_mode = on
verbosity = 1
